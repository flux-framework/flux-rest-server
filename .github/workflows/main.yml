on: [pull_request, push]
name: ci
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  check-pr:
    name: validate commits
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
    - uses: actions/checkout@v7.0.1
      with:
        ref: ${{ github.event.pull_request.head.sha }}
        fetch-depth: 0
    - run: git fetch origin main
    - uses: flux-framework/pr-validator@master

  spelling:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7.0.1
    - name: Check Spelling
      uses: crate-ci/typos@v1.50.2

  python-format:
    name: code formatting
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/setup-python@v7.0.0
      with:
        python-version: "3.10"
    - uses: actions/checkout@v7.0.1
      with:
        ref: ${{ github.event.pull_request.head.sha }}
        fetch-depth: 0
    - name: install linting and formatting deps
      run: pip install -r scripts/requirements-dev.txt
    - name: format and linting checks
      run: pre-commit run --all-files --show-diff-on-failure

  openapi:
    name: validate openapi spec
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-python@v7.0.0
      with:
        python-version: "3.10"
    - uses: actions/checkout@v7.0.1
    # Conformance-only check: no flux required, so no server source argument.
    # The route-coverage cross-check needs flux and runs in the testsuite.
    - name: install spec validation deps
      run: pip install pyyaml openapi-spec-validator
    - name: validate openapi spec
      run: python3 scripts/openapi-check.py spec/v1/openapi.yaml

  ci-checks:
    runs-on: ubuntu-latest
    env:
      FLUX_TEST_TIMEOUT: 300
      FLUX_TESTS_LOGFILE: t
      debug: t
      TAP_DRIVER_QUIET: 1
    timeout-minutes: 30
    strategy:
      matrix:
        include:
        - name: "el8"
          image: "el8"
          env:
            chain_lint: t
        - name: "noble"
          image: "noble"
          env: {}
        - name: "el10"
          image: "el10"
          env:
            DISTCHECK: t
        - name: "fedora40"
          image: "fedora40"
          env: {}
        - name: "coverage"
          image: "noble"
          coverage: true
          env:
            COVERAGE: t
      fail-fast: false
    name: ${{ matrix.name }}
    steps:
    - name: checkout
      uses: actions/checkout@v7.0.1
      with:
        ref: ${{ github.event.pull_request.head.sha }}
        fetch-depth: 0

    - name: docker-run-checks
      env: ${{ matrix.env }}
      run: >
        ./src/test/docker/docker-run-checks.sh
        -j $(nproc)
        -i ${{ matrix.image }}
        --

    - name: coverage report
      if: success() && matrix.coverage
      uses: codecov/codecov-action@v7.0.0
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ./coverage.xml

    - name: annotate errors
      if: failure() || cancelled()
      run: src/test/checks-annotate.sh

  # End-to-end test against a real Flux *system* instance: nginx -> the _ensure
  # helper -> polkit -> systemd socket activation -> per-user setuid -> guest
  # access to the instance.  None of that is reachable from `make check`, which
  # runs the server against a session instance over its own 0600 socket.
  #
  # Separate from ci-checks: it shares no configuration with that matrix and
  # needs a systemd container under podman rather than docker.
  system:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      matrix:
        image: ["el8", "el10"]
      fail-fast: false
    name: system instance (${{ matrix.image }})
    env:
      FLUX_TEST_TIMEOUT: 300
      FLUX_TESTS_LOGFILE: t
      TAP_DRIVER_QUIET: 1
    steps:
    - name: checkout
      uses: actions/checkout@v7.0.1
      with:
        ref: ${{ github.event.pull_request.head.sha }}
        fetch-depth: 0

    # The container runs systemd, which needs PAM (logind, `sudo`) to work
    # inside it; the host's AppArmor profiles block that.  Done here rather
    # than in the test script so that the one thing CI changes about the host
    # is visible, and so a developer's machine is never modified.
    - name: relax host AppArmor for container systemd
      run: |
        sudo apparmor_parser -R /etc/apparmor.d/unix-chkpwd || true
        sudo apparmor_parser -R /etc/apparmor.d/sudo || true

    - name: run system tests
      run: >
        ./src/test/docker/docker-run-system.sh
        -j $(nproc)
        -i fluxrm/flux-core:${{ matrix.image }}

    - name: annotate errors
      if: failure() || cancelled()
      run: src/test/checks-annotate.sh
